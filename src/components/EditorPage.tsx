import Editor from "@/components/Editor";
import Layout from "@/components/Layout";

const EditorPage = () => {
  return (
    <Layout>
      <Editor />
    </Layout>
  );
};

export default EditorPage;
